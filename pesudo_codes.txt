Pesudo Code for Linear Search

procedure LinearSearch(A: List of all the elements, target: The element which to be searched for)
    n = Length(A)

    for i from 0 to n-1:
        if A[i] is equal to traget
            return i // which will be the position of the element which is present
        end if 
    end for 
    return -1 // return -1 if the teaget is not present in the list of arrays 
End procedure 


Pesudo Code for Binary Search 

procedure BinarySearch(A: List of all the elements, target: The element which is to be found )
    mid = left + right/2 // if float round of using the integer point 
    left =0 
    right =0

    while left <= right 
        if traget is equal to A[mid]
            return mid 
        else if taget > A[mid]
            left = mid +1
        else if target < A[mid]
            right = mid -1 
        end if 
    end while 

    return -1 
    
end procedure

procedure bubbleSort(A: List of all the elements)
    for i=0 i<nums.length i++
        for j=0 j<nums.lenth-1-i j++
            if nums[j] > nums[j+1]
                temp = nums[j]
                nums[j] = nums[j+1]
                nums[j+1] = temp
    return nums 
end procedure
